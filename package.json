{
  "name": "pleach-dashboards",
  "version": "0.1.0",
  "private": true,
  "description": "Live business + delivery dashboards (Linear-sourced), deployed on Netlify.",
  "scripts": {
    "snapshot": "node scripts/snapshot.js",
    "dev": "netlify dev",
    "build": "node scripts/snapshot.js"
  }
}
